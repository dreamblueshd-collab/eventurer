This folder mirrors the structure of MiniProject_server_batch18 (`src/entities`).
Current project uses service/repository queries instead of ORM entity classes.
Add entity models here if/when backend is migrated to ORM/Nest style modules.
